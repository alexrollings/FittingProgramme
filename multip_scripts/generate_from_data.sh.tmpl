#!/bin/zsh

source /home/rollings/.zshrc
source_root

cd /data/lhcb/users/rollings/multi_scripts/build/ 
./Fitting -outputDir=${PATH} -toys=${NTOYS} -daughters=${DAUGHTERS} -charge=${CHARGE} -neutral=${NEUTRAL} -inputDir=${INPUT} -bl=${BL} -bh=${BH} -dl=${DL} -dh=${DH} -dpl=${DPL} -dph=${DPH}
